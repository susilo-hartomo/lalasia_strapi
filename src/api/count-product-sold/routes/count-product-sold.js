'use strict';

/**
 * count-product-sold router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::count-product-sold.count-product-sold');
